apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

images:
  - name: ayushbhagat/hello-world-app
    newTag: latest   # Image Updater will update this automatically
